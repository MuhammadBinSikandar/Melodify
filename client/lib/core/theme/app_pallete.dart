// import 'package:flutter/material.dart';

// class Pallete {
//   static const cardColor = Color.fromRGBO(30, 30, 30, 1);
//   static const greenColor = Colors.green;
//   static const subtitleText = Color(0xffa7a7a7);
//   static const inactiveBottomBarItemColor = Color(0xffababab);

//   static const Color backgroundColor = Color.fromRGBO(18, 18, 18, 1);
//   static const Color gradient1 = Color.fromRGBO(187, 63, 221, 1);
//   static const Color gradient2 = Color.fromRGBO(251, 109, 169, 1);
//   static const Color gradient3 = Color.fromRGBO(255, 159, 124, 1);
//   static const Color borderColor = Color.fromRGBO(52, 51, 67, 1);
//   static const Color whiteColor = Colors.white;
//   static const Color greyColor = Colors.grey;
//   static const Color errorColor = Colors.redAccent;
//   static const Color transparentColor = Colors.transparent;

//   static const Color inactiveSeekColor = Colors.white38;
// }
import 'package:flutter/material.dart';

class Pallete {
  static const cardColor = Color(0xFF121212); // Dark card background
  static const greenColor = Color(0xFF1DB954); // Spotify green
  static const subtitleText = Color(0xFFB3B3B3); // Muted grey for subtitles
  static const inactiveBottomBarItemColor = Color(
    0xFF777777,
  ); // Dull grey for inactive nav items

  static const Color backgroundColor = Color(
    0xFF000000,
  ); // Spotify uses full black backgrounds
  static const Color gradient1 = Color(0xFF1ED760); // Light green for gradients
  static const Color gradient2 = Color(0xFF1DB954); // Core Spotify green
  static const Color gradient3 = Color(
    0xFF1AA34A,
  ); // Slightly darker green for depth
  static const Color borderColor = Color(
    0xFF282828,
  ); // Dark grey border for inputs/cards
  static const Color whiteColor = Colors.white; // Pure white
  static const Color greyColor = Color(0xFFB3B3B3); // Muted grey text
  static const Color errorColor = Colors.redAccent; // Keep as is
  static const Color transparentColor = Colors.transparent; // Keep as is

  static const Color inactiveSeekColor = Color(
    0xFF535353,
  ); // Faint progress bar color
}
